import {Message} from "./Message";
import {BaseMessagePurpose} from "../../constants";
import {ActionRequest, CurrentWidgetIdentity} from "../../type";

export function buildSenderMessagePayload<T>(request: ActionRequest, widgetIdentity: CurrentWidgetIdentity, widgetData: T) {

}